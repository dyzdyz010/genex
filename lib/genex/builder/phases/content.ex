defmodule Genex.Builder.Phases.Content do
  @moduledoc """
  Content phase
  """

  @doc """
  Collect content from the content folder, parse the meta data and return a list of models
  """
  def collect(context) do
    context
  end

  @doc """
  Render the content
  """
  def render(context) do
    context
  end
end
